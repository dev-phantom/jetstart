# Netlify Configuration for JetStart (Monorepo)

# Default build - Documentation site
[build]
  base = "docs"
  command = "npm install && npm run build"
  publish = "docs/build"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"

# Web Emulator build context
[context.web]
  base = "."
  command = "npm install && npm run build --workspace=@jetstart/shared && npm run build --workspace=@jetstart/web"
  publish = "packages/web/dist"

[context.web.environment]
  NODE_VERSION = "20"
  NPM_FLAGS = "--legacy-peer-deps"
