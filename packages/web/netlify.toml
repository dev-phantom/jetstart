# Web Emulator configuration
[build]
  command = "cd ../.. && npm install && npm run build --workspace=@jetstart/shared && npm run build --workspace=@jetstart/web"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"